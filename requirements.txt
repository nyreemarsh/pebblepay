# --- SpoonOS & Agents ---
# Install spoon-core (SpoonOS) from GitHub
git+https://github.com/XSpoonAi/spoon-core.git@main

# Dependencies (also declared in spoon-core if it has setup.py/pyproject.toml)
google-generativeai>=0.3.0
python-dotenv>=1.0.0

# --- API Server ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
